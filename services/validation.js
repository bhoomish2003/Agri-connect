module.exports = {
    validateRegisterInput( { username, password, confirmPassword, email } ) {
        const error = {};
        // if(input.password != input.confirmPassword) {
        //     throw new UserInputError('password conformation failed');
        // }
    }
}